<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="3" name="NavigateRecovery">

      <!-- Main navigation pipeline -->
      <PipelineSequence name="NavigateWithReplanning">
        <RateController hz="0.5">
          <RecoveryNode number_of_retries="1" name="ComputePathRecovery">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context"
                        service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>

        <RecoveryNode number_of_retries="1" name="FollowPathRecovery">
          <FollowPath path="{path}" controller_id="FollowPath"/>
          <ClearEntireCostmap name="ClearLocalCostmap-Context"
                      service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>

      </PipelineSequence>

      <!-- Recovery behaviors - escalates through actions one at a time -->
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <!-- Attempt 1: Just clear costmaps and retry -->
          <Sequence name="ClearingRecovery">
            <ClearEntireCostmap name="ClearLocalCostmap-Recovery"
                                service_name="local_costmap/clear_entirely_local_costmap"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Recovery"
                                service_name="global_costmap/clear_entirely_global_costmap"/>
            <Wait wait_duration="2"/>
          </Sequence>
          <!-- Attempt 2: Back up to free itself -->
          <Sequence name="BackupRecovery">
            <BackUp backup_dist="0.10" backup_speed="-0.10"/>
            <Wait wait_duration="2"/>
          </Sequence>
          <!-- Attempt 3: Spin to reorient and rescan -->
          <Sequence name="SpinRecovery">
            <Spin spin_dist="0.262"/>
            <Wait wait_duration="2"/>
          </Sequence>
        </RoundRobin>
      </ReactiveFallback>

    </RecoveryNode>
  </BehaviorTree>
</root>
